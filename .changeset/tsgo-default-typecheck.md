---
"@zakstam/codex-local-component": patch
---

Make TypeScript 7 preview (`tsgo` from `@typescript/native-preview`) the default for package `typecheck` and `build` workflows, and add explicit `typecheck:tsc` fallback scripts for legacy compiler verification.
