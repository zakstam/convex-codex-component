interface:
  display_name: "Codex Package Setup"
  short_description: "Fail-closed setup for codex-runtime"
  default_prompt: "Use $package-setup to onboard this project to @zakstam/codex-runtime with canonical runtime-owned wiring and verification gates."
