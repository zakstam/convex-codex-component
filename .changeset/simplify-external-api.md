---
"@zakstam/codex-local-component": minor
---

Simplify external API: replace `createCodexConvexHost` with `createCodexHost` (register-in-place, clean names, default actor policy). Add `CodexProvider` + `useCodex` hook for React. Remove `react-integration` entry point.
