---
"@zakstam/codex-local-component": patch
---

Fix host surface generation for dispatch-managed Tauri consumers by preserving app-owned `convex/chat.ts` entry modules and requiring a deterministic `actor.userId` fallback in generated wiring smoke scripts.
