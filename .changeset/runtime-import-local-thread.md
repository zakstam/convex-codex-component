---
"@zakstam/codex-local-component": minor
---

Add a canonical runtime API `importLocalThreadToPersistence(...)` for single-call import of local runtime thread history into Convex persistence, and update docs/examples to use this package-owned path instead of app-level hydration logic.
