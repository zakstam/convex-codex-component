{
  "name": "codex-local-cli-example",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:component": "pnpm --filter @zakstam/codex-local-component run build",
    "start": "pnpm run prepare:component && tsx src/index.ts",
    "typecheck": "pnpm run prepare:component && tsc --noEmit"
  },
  "dependencies": {
    "@zakstam/codex-local-component": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "tsx": "^4.20.3",
    "typescript": "^5.7.3"
  }
}
